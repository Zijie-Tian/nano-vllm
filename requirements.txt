# COMPASS RULER Benchmark Requirements
# Python 3.10 required
# CUDA 12.8 required

# PyTorch (install first via pip with CUDA support)
# pip install torch==2.9.1 torchvision==0.24.1 torchaudio==2.9.1 --index-url https://download.pytorch.org/whl/cu128
torch==2.9.1
torchvision==0.24.1
torchaudio==2.9.1

# Transformers and NLP
transformers==4.53.3
tokenizers>=0.15.0
tiktoken==0.12.0
sentencepiece==0.2.1

# NeMo toolkit (for manifest_utils)
nemo-toolkit[asr]==2.6.1
hydra-core>=1.3.0
lightning>=2.0.0
fiddle>=0.3.0

# Triton (for custom kernels)
triton==3.5.1

# Utilities
einops==0.8.1
ninja>=1.11.0
packaging>=24.0
wheel>=0.45.0
setuptools>=80.0.0
pyyaml>=6.0
tqdm>=4.65.0

# flash-attention and flashinfer are installed from source (see docs/ENVIRONMENT_SETUP.md)
